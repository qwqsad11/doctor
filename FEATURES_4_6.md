# 第 4、6 点功能使用说明

## 电子病历处理

1. 在“电子病历”中新建病历，选择患者和门诊、住院或体检模板，填写结构化内容并保存草稿。
2. 在病历详情的“医嘱”标签中开具药物、检查或检验医嘱，填写执行说明。修改和停止操作会保留历史；停止须填写原因。
3. 填写诊断和病历内容后选择“提交审核”，指定一名上级医生。
4. 上级医生登录后可看到指定给自己的病历，填写意见并通过或退回。退回后作者可修改并重新提交。
5. 最终审核通过后，作者或管理员可以归档。审核中、审核通过和已归档病历禁止编辑医嘱与正文，归档不能通过普通更新接口绕过。
6. 只有无医嘱且无审核历史的草稿可以删除。

上级角色沿用系统现有的 `senior_doctor`。如没有可选上级，先注册另一名医生，再由管理员在“管理授权”赋予该角色。作者不能审核自己的病历；上级只能审核明确指定给自己的病历。角色撤销后，旧登录凭证也不能继续审核。

既有病历及状态保留。新的演示数据脚本只生成草稿，不伪造审核和归档记录。

## 患者健康管理

- 制定计划：填写计划名称、个性化目标、健康指导与当前评估等级。
- 健康监测：医生可以代录数据；患者通过专属入口上传血压或血糖，包含测量时间、设备来源和备注。医生详情每 10 秒自动刷新，历史补录不会覆盖更新的最新指标。
- 定时提醒：设置首次时间及重复间隔，0 为一次，1 为每天。后端每 15 秒处理到期任务，自动将消息写入患者入口。任务和消息持久化，重启后补发一次逾期提醒，并推进周期任务；已取消任务不发送。
- 定期评估：填写结论、健康建议、评估等级及下次评估时间，可同步调整计划名称。历史保留调整前计划，健康建议会同步成为患者消息。逾期评估在页面标记。
- 患者入口：在计划详情生成链接，有效期 7 天。患者可查看本计划指导、上传数据和阅读健康提醒。更新或撤销链接后旧链接立即失效。链接令牌通过请求头发送，数据库仅存摘要。
- 消息通过项目内患者入口送达，不接入短信、邮件或微信；患者打开入口后每 10 秒获取新消息。当前部署为本机服务，跨设备使用需要另行配置可访问地址。
- 详情展示最近 300 条活动记录；各表格可在已加载记录中分页。监测数据不会自动给出诊断，评估等级由医生填写。

## 本地运行与验证

当前服务入口为 http://localhost:3000 。在项目目录运行 `powershell -ExecutionPolicy Bypass -File .\start-local.ps1` 可启动本地服务。代码修改后先构建前后端，再重启后端以加载代码和数据库新增字段。

```powershell
cd backend
npm run build
npm run test:workflows
cd ../frontend
npm run type-check
npm run build
```

`test:workflows` 会创建带时间戳的独立测试数据库，在 3101 端口启动测试后端。它覆盖权限、医嘱历史、审核退回、并发审核、归档锁定、患者链接、评估及重启后的定时提醒，结束后只清理本次测试库。测试使用已有 PostgreSQL 连接配置。

可选浏览器回归：在项目根目录运行 `npm install --prefix .runtime/browser --no-save --package-lock=false playwright-core`，保持 3000 端口前端可访问，然后在 backend 目录设置 `$env:WORKFLOW_BROWSER_TEST='1'` 再运行 `npm run test:workflows`。使用本机 Chrome（可通过 CHROME_PATH 指定路径），请求转向独立测试库，截图保存在 .runtime 中。

# 前端环境变量参考模板（复制为 .env.development 使用）
# Vite 会自动加载 .env.development / .env.production

# 后端 API 地址（末尾带 /api/v1）
VITE_APP_API_URL=http://localhost:3001/api/v1

# 应用名称与版本
VITE_APP_NAME=医生服务系统
VITE_APP_VERSION=0.1.0

# Frontend environment variable template (copy to .env.development)
# Vite automatically loads .env.development / .env.production

# Backend API URL (including /api/v1)
VITE_APP_API_URL=/api/v1

# Application name and version
VITE_APP_NAME=Doctor Services
VITE_APP_VERSION=0.1.0
